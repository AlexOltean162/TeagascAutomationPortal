# Server
PORT=3000

# Email (SMTP) - optional.
# If SMTP is not configured, the portal will fall back to opening a mail draft (mailto:).
#
# IMPORTANT:
# - The portal ALWAYS sends from SMTP_FROM.
# - The user-submitted email (if provided) is used only as Reply-To.
#
# Common internal relay (no auth, plaintext SMTP):
SMTP_HOST=smtprelay1.teagasc.net
SMTP_PORT=25
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=

# Optional
SMTP_FROM="Teagasc Automation Portal <no-reply@teagasc.ie>"

# STARTTLS / TLS controls
# Setting SMTP_SECURE=false does not guarantee plaintext; some servers upgrade via STARTTLS.
# Use SMTP_IGNORE_TLS=true to disable STARTTLS (useful for insecure relays).
SMTP_IGNORE_TLS=true
SMTP_REQUIRE_TLS=false
SMTP_TLS_REJECT_UNAUTHORIZED=true

# Diagnostics
SMTP_DEBUG=false
SMTP_LOGGER=false
SMTP_VERIFY_ON_START=false

# Timeouts (ms)
SMTP_CONNECTION_TIMEOUT=120000
SMTP_GREETING_TIMEOUT=30000
SMTP_SOCKET_TIMEOUT=600000
SMTP_DNS_TIMEOUT=30000

# Idea submission routing
IDEA_RECIPIENT=TeagascICTResearchInnovationteam@teagasc.ie
IDEA_SUBJECT_PREFIX="[Automation Idea]"

# Optional CORS (only needed if hosting the frontend separately)
CORS_ORIGIN=
